# ─── Database Configuration ─────────────────────────────────
# Local dev: SQLite (file:./db/custom.db)
# Vercel prod: PostgreSQL (set DATABASE_URL in Vercel dashboard)
DATABASE_URL=file:./db/custom.db

# ─── Production Database (PostgreSQL for Vercel) ─────────────
# Set these in Vercel Environment Variables instead of here
# DATABASE_URL=postgresql://user:pass@host/db?sslmode=require
# DIRECT_URL=postgresql://user:pass@host/db?sslmode=require

# ─── OTP Provider Configuration ─────────────────────────────────
# Real SMS provider only: msg91 | twilio
# NO demo mode - OTP is always sent via real SMS
OTP_PROVIDER=msg91

# ─── MSG91 Configuration ────────────────────────────────────────
# Sign up at https://msg91.com
# Auth Key: Required (from Dashboard → API Keys)
# Template ID: Optional - leave empty to use MSG91's built-in OTP service
#   (Built-in service uses MSG91's DLT-registered sender = guaranteed delivery)
#   (Custom template requires your own DLT-approved sender ID)
# Sender ID: Not used - MSG91's built-in OTP service uses its own DLT-registered sender
MSG91_AUTH_KEY=528840AMmrbUJi0U4b6a2e3858P1
# MSG91_TEMPLATE_ID - LEAVE EMPTY!
# Your template 6a2e3af8cda93304790f2f32 is INVALID (shows "Template ID Missing or Invalid Template" in MSG91 logs)
# Without template, MSG91 uses its BUILT-IN OTP service with pre-approved DLT sender = guaranteed delivery
MSG91_TEMPLATE_ID=

# ─── Twilio Configuration (optional alternative) ────────────────
# Sign up at https://twilio.com
# Option A: Twilio Verify (recommended)
#   1. Create Verify Service: Verify → Services → Create
#   2. Set TWILIO_VERIFY_SERVICE_SID
# Option B: Direct SMS
#   1. Buy a phone number
#   2. Set TWILIO_PHONE_NUMBER
# Pricing: ~₹0.70-1.00 per SMS | $15 free trial credit
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=
TWILIO_VERIFY_SERVICE_SID=
